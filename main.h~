#ifndef MAIN_H
#define MAIN_H

#include <mega128a.h> 

void ReadCurrent(void);
void ReadVoltage(void);
void device_state(int state);

typedef struct {
    char dataID;
    char dataType;
    float value;
}DATASET;

#endif
